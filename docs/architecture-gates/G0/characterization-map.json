{
  "version": "1.0.0",
  "gate": "G0",
  "coverage": [
    { "behavior": "fixture route/retry", "file": "server/g0-fixture-characterization.test.ts", "title": "loads registered workspace and error-retry fixtures to characterize route and recovery semantics" },
    { "behavior": "chat", "file": "server/app.test.ts", "title": "calls the provider and stores messages with a manifest" },
    { "behavior": "branch", "file": "server/app.test.ts", "title": "creates, moves and merges a formal discussion branch" },
    { "behavior": "edit/resend", "file": "server/app.test.ts", "title": "creates traceable Edit & Resend and Regenerate event versions without overwriting history" },
    { "behavior": "regenerate", "file": "server/app.test.ts", "title": "preserves pinned context over budget and freezes a new immutable manifest for Regenerate" },
    { "behavior": "Stop e2e", "file": "e2e/provider-stream.e2e.test.ts", "title": "aborts the upstream provider on Stop and leaves persisted history unchanged" },
    { "behavior": "backend retry", "file": "server/app.test.ts", "title": "keeps a failed stream uncommitted and retries without overwriting history" },
    { "behavior": "UI retry", "file": "src/App.test.tsx", "title": "shows an explicit Retry action after a failed request" },
    { "behavior": "file", "file": "server/app.test.ts", "title": "uploads a text attachment, displays its metadata and includes its content in the provider call" },
    { "behavior": "archive/restore", "file": "server/app.test.ts", "title": "archives graph nodes idempotently without losing their complete history" },
    { "behavior": "controlled purge", "file": "server/app.test.ts", "title": "isolates physical purge behind archived state, explicit confirmation and an audit receipt" },
    { "behavior": "merge/archive", "file": "server/app.test.ts", "title": "creates graph nodes and semantic edges while retaining archived nodes" },
    { "behavior": "provider selection", "file": "server/app.test.ts", "title": "runs three provider profiles through the same Runtime contract" }
  ]
}
