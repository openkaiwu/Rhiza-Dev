/**
 * Compatibility export while the current provider adapter moves behind the
 * execution-runtime port. It deliberately preserves the existing constructor
 * and streaming behaviour.
 */
export { ProviderRuntime } from '../provider-runtime';
